# Security: Prevent viewing of source code files
<FilesMatch "\.(js|json|env|log|md)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# Security: Prevent viewing of dotfiles
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>

# Disable directory listing
Options -Indexes

# NOTE: The actual Node.js application startup is handled by the cPanel "Setup Node.js App"
# feature, which will use Phusion Passenger. You typically do not need custom rewrite rules
# here if you configure that correctly.
